\documentclass[12pt]{article}
\usepackage{hyperref}

\usepackage{graphicx}

\usepackage{mathtools}
\DeclarePairedDelimiter\ceil{\lceil}{\rceil}
\DeclarePairedDelimiter\floor{\lfloor}{\rfloor}

\usepackage[dvipsnames]{xcolor}

\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}

\newcommand{\Mod}[1]{\ (\mathrm{mod}\ #1)}


\usepackage{listings}
\lstset{language=C++,
  basicstyle=\fontsize{8}{13}\ttfamily,
  keywordstyle=\color{blue}\ttfamily,
  stringstyle=\color{red}\ttfamily,
  commentstyle=\color{BrickRed}\ttfamily,
  morecomment=[l][\color{magenta}]{\#}
}

\usepackage{breqn}
\usepackage[]{algorithm2e}
\usepackage{physics}

\newcommand\fnurl[2]{%
  \href{#2}{#1}\footnote{\url{#2}}%
}

\newcommand{\Four}{\mathcal{F}}
\renewcommand{\(}{\left(}
\renewcommand{\)}{\right)}
%% \renewcommand{\{}{\left{}
%% \renewcommand{\}}{\right}}


\title{API design for rocFFT/hipFFT}
\author{The rocFFT Team}



\def\no#1{\nobreak{#1}} % stop equation breaking in dmath

\begin{document}
\maketitle

\section{Introduction}

FIXME

\noindent
\texttt{rocFFT}.

\noindent
\texttt{hipFFT}: obligatory XKCD: \url{https://xkcd.com/927/}

\section{DAG}


\begin{center}
  \includegraphics[width=0.9\textwidth]{implementdag.pdf}
\end{center}

\newpage
\subsection{Fields}
\label{ssfields}

\begin{center}
  \begin{tabular}{|l|l|l|}
    \hline
    Dependencies & Status & Options \\
    \hline
    None & Experimental implementation in ROCm 6.0  & None \\
    \hline
  \end{tabular}
\end{center}

\subsubsection{rocFFT API change}

\noindent
rocFFT API change:
\begin{itemize}
\item
  \begin{lstlisting}
typedef struct rocfft_field_t* rocfft_field;
rocfft_status rocfft_field_create(rocfft_field* field);
rocfft_status rocfft_field_destroy(rocfft_field field);
  \end{lstlisting}
  \begin{lstlisting}
rocfft_status rocfft_plan_description_add_infield(
                  rocfft_plan_description description,
                  rocfft_field            field);
rocfft_status rocfft_plan_description_add_outfield(
                  rocfft_plan_description description,
                  rocfft_field field);
  \end{lstlisting}
\item The field-adding functions can be called once per plan.
\end{itemize}

\subsubsection{hipFFT API change for cuFFT parity}

No changes.

\subsubsection{hipFFT API change for rocFFT parity}
\begin{lstlisting}
  FIXME
\end{lstlisting}


\newpage
\subsection{Bricks}
\label{ssbricks}


\begin{center}
  \begin{tabular}{|l|l|l|}
    \hline
    Dependencies & Status & Options \\
    \hline
    Field (\ref{ssfields}) & Experimental implementation in ROCm 6.0  & FIXME \\
    \hline
  \end{tabular}
\end{center}


\subsubsection{rocFFT API change}
rocFFT API additions:
\begin{lstlisting}
typedef struct rocfft_brick_t* rocfft_brick;
rocfft_status rocfft_brick_create(rocfft_brick* brick,
                                  const size_t* field_lower,
                                  const size_t* field_upper,
                                  const size_t* brick_stride,
                                  size_t        dim,
                                  int           deviceID);
rocfft_status rocfft_brick_destroy(rocfft_brick brick);
rocfft_status rocfft_field_add_brick(rocfft_field field, rocfft_brick brick);
\end{lstlisting}

\subsubsection{hipFFT API }
\begin{lstlisting}
FIXME
\end{lstlisting}

\newpage
\subsection{Single-Process Multi-GPU}


\begin{center}
  \begin{tabular}{|l|l|l|}
    \hline
    Dependencies & Status & Options \\
    \hline
    Bricks (\ref{ssbricks}) & Experimental implementation in ROCm 6.0 & Specifying the list of GPUs \\
    \hline
  \end{tabular}
\end{center}

Notes:
\begin{itemize}
\item We are not handling the shuffled case from cuFFTXt; it's silly
  and no one uses it.  We could come up with some general equivalent
  in arbitrary dimensions, but we'd have to see someone that would
  actually use it.
\end{itemize}

   



\subsubsection{rocFFT API change}
\begin{itemize}
\item implied by brick-device association.
\end{itemize}
Optional rocFFT API change:
\begin{lstlisting}
rocfft_status rocfft_plan_description_set_devices(rocfft_plan_description description,
                                                  void *devices,
                                                  size_t number_of_devices);
\end{lstlisting}
Also note that the decomposition dimension has be lower than the
transform dimension.

\subsubsection{hipFFT API change for cuFFT parity}
\texttt{hipfftXt.h}
\begin{lstlisting}
hipfftResult hipfftXtSetGPUs(hipfftHandle plan, int count, int* gpus);
typedef enum hipfftXtSubFormat_t
{
    HIPFFT_XT_FORMAT_INPUT             = 0x00,
    HIPFFT_XT_FORMAT_OUTPUT            = 0x01,
    HIPFFT_XT_FORMAT_INPLACE           = 0x02,
    HIPFFT_XT_FORMAT_INPLACE_SHUFFLED  = 0x03,
    HIPFFT_XT_FORMAT_1D_INPUT_SHUFFLED = 0x04,
    HIPFFT_FORMAT_UNDEFINED            = 0x05
} hipfftXtSubFormat;
hipfftResult hipfftXtMalloc(hipfftHandle      plan,
                            hipLibXtDesc**    desc,
                            hipfftXtSubFormat format)
hipfftResult hipfftXtMemcpy(hipfftHandle     plan,
                            void*            dest,
                            void*            src,
                            hipfftXtCopyType type);
hipfftResult hipfftXtFree(hipLibXtDesc* desc);
// C2C, R2C, ...
hipfftResult hipfftXtExecDescriptorC2C(hipfftHandle  plan,
                                       hipLibXtDesc* input,
                                       hipLibXtDesc* output,
                                       int           direction)
                            
\end{lstlisting}

\texttt{hipfftlibxt.h}
\begin{lstlisting}
typedef struct hipXtDesc_t
{
    int version;
    int nGPUs;
    int GPUs[MAX_HIP_DESCRIPTOR_GPUS];
    void* data[MAX_HIP_DESCRIPTOR_GPUS];
    size_t size[MAX_HIP_DESCRIPTOR_GPUS];
    void* hipXtState;
} hipXtDesc;
typedef enum hiplibFormat_t
{
    HIPLIB_FORMAT_HIPFFT    = 0x0,
    HIPLIB_FORMAT_UNDEFINED = 0x1
} hiplibFormat;
typedef struct hipLibXtDesc_t
{
    int version;
    hipXtDesc* descriptor;
    hiplibFormat library;
    int subFormat;
    void* libDescriptor;
} hipLibXtDesc;
typedef enum hipfftXtCopyType_t
{
    HIPFFT_COPY_HOST_TO_DEVICE   = 0x00,
    HIPFFT_COPY_DEVICE_TO_HOST   = 0x01,
    HIPFFT_COPY_DEVICE_TO_DEVICE = 0x02,
    HIPFFT_COPY_UNDEFINED        = 0x03
} hipfftXtCopyType;
\end{lstlisting}


\subsubsection{hipFFT API change for rocFFT parity}
We would have to add bricks.
\begin{lstlisting}
FIXME
\end{lstlisting}

\newpage
\subsubsection{Multi-Process Multi-GPU}
\label{ssmpmgpu}

\begin{center}
  \begin{tabular}{|l|l|l|}
    \hline
    Dependencies & Status & Options \\
    \hline
    Bricks (\ref{ssbricks}) & Scoped for development starting in 6.2  & None \\
    \hline
  \end{tabular}
\end{center}


\subsubsection{rocFFT API change}
\begin{lstlisting}
typedef enum rocfft_comm_type_t {
} rocfft_comm_type;
rocfft_status rocfft_plan_description_set_comm(
    rocfft_plan_description description,
    const enum_type_for_mpi_or_schmem comm_type,
    void* comm_handle);
\end{lstlisting}

\subsubsection{hipFFT API change}
\noindent
hipFFT API additions:
\texttt{cuFFTMp.h}
\begin{lstlisting}
typedef enum cufftMpCommType_t {
    CUFFT_COMM_MPI = 0x00,
    CUFFT_COMM_NONE = 0x01
} cufftMpCommType;
cufftResult cufftMpAttachComm(cufftHandle plan,
                                       cufftMpCommType comm_type,
                                       void *comm_handle);
\end{lstlisting}
\texttt{cufftXp.h}
\begin{lstlisting}
cufftResult cufftXtSetDistribution(cufftHandle plan, 
                                   int rank, 
                                   const long long int* lower_input,  
                                   const long long int* upper_input,
                                   const long long int* lower_output,
                                   const long long int* upper_output,
                                   const long long int* strides_input, 
                                   const long long int* strides_output);
typedef enum hipfftXtSubFormat_t
{
  // other enums...
  CUFFT_XT_FORMAT_DISTRIBUTED_INPUT = 0x05,
  CUFFT_XT_FORMAT_DISTRIBUTED_OUTPUT = 0x06,
  CUFFT_FORMAT_UNDEFINED = 0x07
} hipfftXtSubFormat;
\end{lstlisting}


\newpage
\subsection{Multi-Process Multi-GPU with MPI}

\begin{center}
  \begin{tabular}{|l|l|l|}
    \hline
    Dependencies & Status & Options \\
    \hline
    Multi-proc (\ref{ssmpmgpu}) & Scoped for development starting in 6.2    & None \\
    \hline
  \end{tabular}
\end{center}


Notes: we will have a huge problem with the MPI ABI.  CF:
\url{https://www.youtube.com/watch?v=kNk9efs4m-c}.  There are a few cuFFT
features that seem unimportant:
\begin{itemize}
\item Reshape API: it seems that \texttt{cufftMpCreateReshape} isn't
  used anywhere on github except in CUDA library samples.
\end{itemize}

\subsubsection{rocFFT API change}
\begin{lstlisting}
typedef enum rocfft_comm_type_t {
  rocfft_comm_mpi = 0,
} rocfft_comm_type;
\end{lstlisting}

\subsubsection{hipFFT API change}
\noindent
hipFFT API additions:
\begin{lstlisting}
FIXME
\end{lstlisting}

\newpage
\subsection{Multi-Process Multi-GPU with rocSHMEM}

\begin{center}
  \begin{tabular}{|l|p{4cm}|l|}
    \hline
    Dependencies & Status & Options \\
    \hline
    Multi-proc (\ref{ssmpmgpu}) & Unscoped.   Working on performance model for BU justification.    & None \\
    \hline
  \end{tabular}
\end{center}

\subsubsection{rocFFT API change}
\noindent
rocFFT API additions:
\begin{lstlisting}
typedef enum rocfft_comm_type_t {
    rocfft_comm_shmem = 1,
} rocfft_comm_type;
\end{lstlisting}

\subsubsection{hipFFT API change}
\noindent
hipFFT API additions:
\begin{lstlisting}
FIXME
\end{lstlisting}

\newpage
\subsection{Sparse Data}


\begin{center}
  \begin{tabular}{|l|l|l|}
    \hline
    Dependencies & Status & Options \\
    \hline
    Bricks (\ref{ssbricks}) & Unscoped   & Explicitly specify zero-bricks \\
    \hline
  \end{tabular}
\end{center}

\subsubsection{rocFFT API change}
\begin{itemize}
\item Behaviour change: Non-specified bricks are treated as zero.
\end{itemize} 

\subsubsection{hipFFT API change}
\noindent
hipFFT API changes:
\begin{lstlisting}
  FIXME
\end{lstlisting}


\newpage
\subsection{One-Way Scalar Spectral Ops}
\label{sss1wayspecops}

\begin{center}
  \begin{tabular}{|l|l|l|}
    \hline
    Dependencies & Status & Options \\
    \hline
    None & Unscoped   & Shared data size \\
    \hline
  \end{tabular}
\end{center}
\noindent

\subsubsection{rocFFT API change}
\begin{lstlisting}
typedef struct rocfft_specop_t* rocfft_specop;
rocfft_status rocfft_specop_create(rocfft_specop* specop);
rocfft_status rocfft_specop_destroy(rocfft_specop specop);
rocfft_status rocfft_specop_set_op(rocfft_specop specop, char* opstring);
rocfft_status rocfft_plan_description_set_readop(
                                  rocfft_plan_description description,
                                  const rocfft_specop specop);
rocfft_status rocfft_plan_description_set_writeop(
                                  rocfft_plan_description description,
                                  const rocfft_specop specop);
\end{lstlisting}
API option:
\begin{lstlisting}
rocfft_status rocfft_specop_set_shared_size(rocfft_specop specop, size_t bytes);
\end{lstlisting}


\subsubsection{hipFFT API change}

\noindent
hipFFT API additions:
\begin{lstlisting}
  FIXME
\end{lstlisting}

\newpage
\subsection{One-Way Multi-valued Spectral Ops}
\label{ssvecspecops}

\begin{center}
  \begin{tabular}{|l|l|p{5cm}|}
    \hline
    Dependencies & Status & Options \\
    \hline
    One-way specops (~\ref{sss1wayspecops}) & Unscoped   & Fine-grained field specification \\
    \hline
  \end{tabular}
\end{center}
\noindent

\subsubsection{rocFFT API change}
\begin{itemize}
\item The add-field functions can be called multiple times without error.
\end{itemize}
\begin{lstlisting}
rocfft_status rocfft_specop_set_fields(rocfft_specop specop,
                                       size_t ninputs,
                                       size_t* input_list,
                                       size_t outputs,
                                       size_t* output_list);
  FIXME
\end{lstlisting}
API options:
\begin{itemize}
\item Assume all fields are used:
  \begin{lstlisting}
rocfft_status rocfft_specop_set_fields(rocfft_specop  specop, size_t ninputs, size_t outputs);}
  \end{lstlisting}
\item
  \begin{lstlisting}
rocfft_status rocfft_specop_set_types(rocfft_specop specop,
                                     field_type_enum data_types);
rocfft_status rocfft_specop_set_types(rocfft_specop specop,
                                      size_t data_size);
  \end{lstlisting}
\item
  \begin{lstlisting}
rocfft_status rocfft_specop_set_fields(rocfft_specop  specop, size_t ninputs, size_t outputs);}
  \end{lstlisting}
\end{itemize}

\subsubsection{hipFFT API change}
hipFFT API additions:
\begin{lstlisting}
  FIXME
\end{lstlisting}


\newpage
\subsection{Round Trip Multi Spectral Ops Without Padding}
\label{ssroundspecops}


\begin{center}
  \begin{tabular}{|l|l|p{5cm}|}
    \hline
    Dependencies & Status & Options \\
    \hline
    Multi-valued specops (~\ref{ssvecspecops}) & Unscoped   & FIXME \\
    \hline
  \end{tabular}
\end{center}
\noindent


\subsubsection{rocFFT API change}
\begin{lstlisting}
rocfft_transform_type_complex_forward_roundtrip,
    rocfft_transform_type_complex_inverse_roundtrip,
    rocfft_transform_type_real_forward_roundtrip,
    rocfft_transform_type_real_inverse_roundtrip,
    FIXME
rocfft_status rocfft_plan_description_set_midop(
               rocfft_plan_description description,
               const rocfft_specop specop);
\end{lstlisting}
Optional additions:
\begin{lstlisting}
rocfft_status rocfft_desc_set_precision_roundtrip(
                   rocfft_plan_description description,
                   size_t numprecision_in,
                   rocfft_precision* precision_in,
                   size_t numprecision_out,
                   rocfft_precision* precision_out);
\end{lstlisting}



\subsubsection{hipFFT API }
hipFFT API additions:
\begin{lstlisting}
  FIXME
\end{lstlisting}
\newpage
\subsection{Round Trip Multi Spectral Ops With Padding}



\begin{center}
  \begin{tabular}{|l|l|p{5cm}|}
    \hline
    Dependencies & Status & Options \\
    \hline
    Multi-valued specops (~\ref{ssvecspecops}) & Unscoped   & FIXME \\
    \hline
  \end{tabular}
\end{center}
\noindent


\subsubsection{rocFFT API change}
\begin{lstlisting}
rocfft_status rocfft_specop_set_conv_params(rocfft_specop specop,
                                            size_t conv_order,
                                             conv_enum_type conv_type);
\end{lstlisting}

\subsubsection{hipFFT API }
hipFFT API additions:
\begin{lstlisting}
  FIXME
\end{lstlisting}


\newpage
\subsection{Multi-precision and multi-resolution}


\begin{center}
  \begin{tabular}{|l|l|l|}
    \hline
    Dependencies & Status & Options \\
    \hline
    Various (TODO) & Unscoped  & FIXME \\
    \hline
  \end{tabular}
\end{center}

\subsubsection{rocFFT API change}
\begin{lstlisting}
rocfft_status rocfft_field_set_type(rocfft_field field, char* data_type);
ROCFFT_EXPORT rocfft_status rocfft_specop_set_types(rocfft_specop specop,
                                                   field_type_enum data_types);
ROCFFT_EXPORT rocfft_status rocfft_specop_set_types(rocfft_specop specop,
                                                   size_t data_size);
rocfft_status rocfft_field_set_properties(rocfft_field field,
                                          rocfft_precision precisions,
                                          bool isconst);
rocfft_status rocfft_field_dims(rocfft_field field,
                                size_t* dims,
                                size_t dim,
                                field_type_enum field_type);

rocfft_status rocfft_desc_set_communication_precision(rocfft_plan_description description,
                                                      size_t numprecision,
                                                      rocfft_precision* precisions);
\end{lstlisting}

\subsubsection{hipFFT API }
FIXME

\end{document}
