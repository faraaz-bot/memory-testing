Do compile the document, first run doasy.sh.

Then, run latexmk -pdf on the tex file
Then, find any files that looks like 
 rocfft_mpi_hpcsync2025-05-1.asy
and run asy on those.
Then run latexmk -pdf on the tex file again.
